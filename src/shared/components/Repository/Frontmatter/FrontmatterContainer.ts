import styled from 'styled-components/macro';

export const FrontmatterContainer = styled.h2`
  font-size: 1.8rem;
  color: #ffffff;
  font-weight: 400;
  display: grid;
  grid-template-columns: max-content 1fr;
  align-items: center;
  gap: 0.5ch;
`;
