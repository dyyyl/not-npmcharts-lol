import styled from 'styled-components/macro';

export const HintItem = styled.li`
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  padding: 1rem 2.4rem;

  &:hover {
    cursor: pointer;
    background: #ececf6;
  }
`;
